<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AdvancedBudgetManager.view.window.ResetPasswordWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdvancedBudgetManager.view.window"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:AdvancedBudgetManager.utils"
    mc:Ignorable="d"
    Title="ResetPasswordWindow">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock Text="Reset password" FontSize="36" Margin="50" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <PasswordBox Header="New password" x:Name="NewPasswordBox" PlaceholderText="Enter your new password" PasswordChar="*" helpers:PasswordBoxHelper.BindPassword="true" helpers:PasswordBoxHelper.BoundPassword="{x:Bind resetPasswordViewModel.NewPassword, Mode=TwoWay}" Margin="4" Width="280" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <PasswordBox Header="Confirm password" x:Name="ConfirmPasswordBox" PlaceholderText="Confirm your password" PasswordChar="*" Margin="4" Width="280" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="Reset password" x:Name="ResetPasswordButton" Click="ResetPasswordButton_Click" Background="DodgerBlue" Foreground="White" Margin="20" Width="125" Grid.Row="0" Grid.Column="0"/>
                <Button Content="Cancel" x:Name="ResetPasswordCancelButton" Background="DodgerBlue" Foreground="White" Margin="20" Width="125" Grid.Row="0" Grid.Column="1"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
