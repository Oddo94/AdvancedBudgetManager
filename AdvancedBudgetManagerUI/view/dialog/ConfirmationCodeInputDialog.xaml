<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AdvancedBudgetManager.view.dialog.ConfirmationCodeInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:AdvancedBudgetManager.view.dialog"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Confirmation code"
    PrimaryButtonText="Check code"
    SecondaryButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel>
        <TextBox x:Name="ConfirmationCodeTextBox" Header="Enter the confirmation code" PlaceholderText="Confirmation code received on email" Text="{x:Bind emailConfirmationViewModel.InputConfirmationCode, Mode=TwoWay}" TextAlignment="Center" Width="250" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TeachingTip x:Name="InvalidConfirmationCodeTip" Title="Invalid confirmation code" Subtitle="Please try again!" Target="{x:Bind ConfirmationCodeTextBox}" PreferredPlacement="LeftBottom" IsLightDismissEnabled="true" IsOpen="false"/>
    </StackPanel>
</ContentDialog>
