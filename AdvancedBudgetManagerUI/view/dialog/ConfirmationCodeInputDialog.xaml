<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="AdvancedBudgetManager.view.dialog.ConfirmationCodeInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdvancedBudgetManager.view.dialog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d"
    Title="Confirmation code"
    PrimaryButtonText="Check code"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel>
        <TextBox Header="Enter the confirmation code" PlaceholderText="Confirmation code received on email" Text="{x:Bind emailConfirmationViewModel.InputConfirmationCode, Mode=TwoWay}" TextAlignment="Center" x:Name="ConfirmationCodeTextBox" Width="250" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <!--<Button Content="Check code" x:Name="CheckConfirmationCodeButton" Foreground="DodgerBlue"  HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16"/>
        <Button Content="Cancel" x:Name="CancelCodeConfirmationButton" Foreground="DodgerBlue"  HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="16"/>-->
    </StackPanel>
</ContentDialog>
